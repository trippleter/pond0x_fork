"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../utils");
function parse(step) {
    const stack = (0, utils_1.shallowCopyStack2)(step.stack);
    if (stack.length < 2) {
        throw new Error("[hardhat-tracer]: Faulty MSTORE");
    }
    const offset = (0, utils_1.parseBytes32)(stack.pop());
    const value = (0, utils_1.parseBytes32)(stack.pop());
    return {
        opcode: "MSTORE8",
        params: {
            offset,
            value,
        },
    };
}
function format(item) {
    return `${(0, utils_1.colorLabel)("[MSTORE8]")} ${(0, utils_1.colorMstore)(item.params.offset)} ← ${(0, utils_1.colorValue)(item.params.value)}`;
}
exports.default = { parse, format };
//# sourceMappingURL=mstore8.js.map